---

# yaml-language-server: $schema=./router-config.schema.json

plugins:
  experimental.include_subgraph_errors:
    all: true
headers:
  all:
    - propagate:
        named: authorization
    - propagate:
        named: user-agent
